export enum Collection {
  Users = 'users',
  Scores = 'scores',
  Settings = 'settings',
}

export const COLLECTIONS = [Collection.Users, Collection.Scores, Collection.Settings];
export const DB_NAME = 'Eka-G';

export default {};
